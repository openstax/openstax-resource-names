{
  "private": true,
  "name": "@project/openstax-resource-names",
  "scripts": {
    "ci:spelling": "git ls-files -- . ':!:packages/' | yarn cspell -c ./cspell.json --file-list stdin",
    "ci:packages": "yarn workspaces run ci",
    "ci": "CI=true npm-run-all ci:*"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cspell": "^6.12.0",
    "npm-run-all": "^4.1.5"
  }
}
